import React from 'react';
import AuthForm from '../forms/AuthForm';

const Login = () => {
    return (
        <div>
            <AuthForm type="login"/>
        </div>
    );
};

export default Login;
